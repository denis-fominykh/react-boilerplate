const apiBase = 'https://swapi.dev/api';

export const API = {
  ROOT: apiBase,
  FILMS: `${apiBase}/films`,
  PEOPLE: `${apiBase}/people`,
  PLANETS: `${apiBase}/planets`,
  SPECIES: `${apiBase}/species`,
  VEHICLES: `${apiBase}/vehicles`,
  STARSHIPS: `${apiBase}/starships`,
};
